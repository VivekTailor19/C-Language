//Write C program to remove spaces, blanks from a string.

#include<stdio.h>
#include<string.h>
main()
{
	char a[100];
	int i,j,n;
	printf("Enter the Sentence::");
	gets(a);
	n=strlen(a);
	
	for(i=0;i<n;i++)
	{
		if(a[i]!=' ')
		{
			printf("%c",a[i]);
		}
	}
	
}
